/// @function percent(number, percent);
/// @param {real} number Starting value
/// @param {real} percent What percent of the starting value

function percent(_number, _percent){
    _base = (_number / 100);
    return (_base * _percent);
};