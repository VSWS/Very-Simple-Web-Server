/// @description size_count(number to add to total sends)
/// @param number to add to total sends
function size_count(argument0) {
	//adds the amount to the total MB sent counter

	counted_size += argument0;


}
